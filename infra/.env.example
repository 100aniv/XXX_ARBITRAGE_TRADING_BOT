# Arbitrage-Lite Docker Environment Template
# ============================================
# 
# 사용 방법:
# 1. 이 파일을 복사: cp .env.example .env
# 2. 실제 값으로 수정
# 3. docker-compose up 시 자동 로딩
#
# ⚠️ 주의: 실거래 모드에서는 강력한 비밀번호 사용 필수
#

# ============================================================================
# 데이터베이스 (PostgreSQL + TimescaleDB)
# ============================================================================
DB_HOST=postgres
DB_PORT=5432
DB_NAME=arbitrage
DB_USER=arbitrage
DB_PASSWORD=change_me_to_strong_password

# ============================================================================
# Redis
# ============================================================================
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================================================
# arbitrage-core 설정
# ============================================================================

# 운영 모드
# false: paper/simulation mode (기본, 안전)
# true: 실거래 모드 (LiveGuard, Safety 활성화)
LIVE_MODE=false

# 안전 모드 (LiveGuard, Safety 모듈)
# true: 활성화 (기본, 권장)
# false: 비활성화 (테스트 전용)
SAFETY_MODE=true

# 로깅
LOG_LEVEL=INFO
LOG_DIR=/app/logs

# 설정 파일 경로
CONFIG_PATH=/app/config/base.yml

# ============================================================================
# Exchange API (실거래 모드에서만 필요)
# ============================================================================

# Upbit
UPBIT_API_KEY=your_upbit_api_key_here
UPBIT_SECRET_KEY=your_upbit_secret_key_here

# Binance
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_SECRET_KEY=your_binance_secret_key_here

# ============================================================================
# Dashboard 설정
# ============================================================================
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8001
DASHBOARD_WORKERS=4

# ============================================================================
# 모니터링 (Prometheus, Grafana)
# ============================================================================

# Prometheus
PROMETHEUS_RETENTION=15d

# Grafana
GF_SECURITY_ADMIN_PASSWORD=admin

# ============================================================================
# 리소스 제한 (선택)
# ============================================================================

# arbitrage-core CPU/메모리 제한
# CORE_CPU_LIMIT=2
# CORE_MEMORY_LIMIT=2G
# CORE_CPU_RESERVATION=1
# CORE_MEMORY_RESERVATION=1G

# dashboard CPU/메모리 제한
# DASHBOARD_CPU_LIMIT=1
# DASHBOARD_MEMORY_LIMIT=1G
# DASHBOARD_CPU_RESERVATION=0.5
# DASHBOARD_MEMORY_RESERVATION=512M

# ============================================================================
# 고급 설정
# ============================================================================

# Python 환경
PYTHONUNBUFFERED=1
APP_ENV=docker

# 타임존
TZ=Asia/Seoul

# ============================================================================
# 실거래 모드 체크리스트
# ============================================================================
#
# LIVE_MODE=true로 변경하기 전에 반드시 확인하세요:
#
# 1. [ ] API 키 설정 확인 (UPBIT_API_KEY, UPBIT_SECRET_KEY 등)
# 2. [ ] LiveGuard 설정 확인 (max_position_size, max_daily_loss)
# 3. [ ] Safety 모듈 활성화 (SAFETY_MODE=true)
# 4. [ ] 포지션 크기 제한 설정
# 5. [ ] 일일 손실 제한 설정
# 6. [ ] 긴급 정지 버튼 테스트
# 7. [ ] 대시보드 모니터링 준비
# 8. [ ] 로그 모니터링 준비
# 9. [ ] 백업 전략 수립
# 10. [ ] 팀 공지 및 승인
#
# ============================================================================
