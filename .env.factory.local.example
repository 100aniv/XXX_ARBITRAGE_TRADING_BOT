# Factory-only local environment template (never commit real secrets)
# Copy to .env.factory.local and fill values locally.

# LLM keys (required for factory_run without --do-command override)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Container-network service endpoints
POSTGRES_HOST=arbitrage-postgres
POSTGRES_PORT=5432
REDIS_HOST=arbitrage-redis
REDIS_PORT=6379

# Optional agent settings
# Leave model names blank to use ticket risk policy.
AIDER_MODEL=
CLAUDE_CODE_MODEL=
# Maximum allowed tier per agent: low|mid|high
AIDER_MODEL_MAX_TIER=mid
CLAUDE_CODE_MODEL_MAX_TIER=mid
